const config = {
  // host: 'http://localhost:3499',
  host: 'http://vote.jianglaiwang.com',
  // host: '',
  filePath: '/api/upload_files',
  intervalTime: 60,
  version: '1.0',
  /**
   * @description 默认打开的首页的路由name值，默认为home
  */
  homeName: 'home',
}

export default config
